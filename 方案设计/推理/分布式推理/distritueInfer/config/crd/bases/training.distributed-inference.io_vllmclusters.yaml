---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vllmclusters.training.distributed-inference.io
spec:
  group: training.distributed-inference.io
  names:
    kind: VLLMCluster
    listKind: VLLMClusterList
    plural: vllmclusters
    singular: vllmcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VLLMCluster is the Schema for the vllmclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VLLMClusterSpec defines the desired state of VLLMCluster.
            properties:
              image:
                default: nginx:latest
                description: Image specifies the container image
                type: string
              port:
                default: 8080
                description: Port for the service
                format: int32
                type: integer
              replicas:
                default: 1
                description: Replicas specifies the number of StatefulSet replicas
                  (distributed instances)
                format: int32
                minimum: 1
                type: integer
            type: object
          status:
            description: VLLMClusterStatus defines the observed state of VLLMCluster.
            properties:
              phase:
                description: Phase indicates the current phase
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready replicas
                format: int32
                type: integer
              replicas:
                description: Replicas is the current number of replicas
                format: int32
                type: integer
              selector:
                description: |-
                  Selector is the label selector for the pods managed by this VLLMCluster
                  Used by the scale subresource
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
